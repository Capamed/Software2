<?php
    $nombre = $_POST['nombre'];
    $nickname = $_POST['nickname'];
    $password = $_POST['password'];
    $repassword = $_POST['repassword'];
    $direccion = $_POST['direccion'];
    $telefono = $_POST['telefono'];
    $email = $_POST['email'];
    $reqlen = strlen($nombre)*strlen($nickname)*strlen($password)*strlen($repassword)*strlen($direccion)*strlen($telefono)*strlen($email);

    if ($reqlen>0){
        if($password==$repassword){
            require("connect_db");

            $password = md5 ($password);

            $sql="INSERT INTO empresa (`nombreE`, `nicknameE`, `passwordE`, `direccionE`, `telefonoE`, `emailE`) VALUES ('$nombre','$nickname','$password','$direccion','$telefono','$email')";


            mysqli_query($enlace,$sql);

            mysqli_close($enlace);



        }else{
            echo 'Por favor introduzca dos contraseñas identicas';
        }
    }else{
        echo 'Por favor rellene todo los campos requeridos';
    }


